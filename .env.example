# Root environment file consumed by docker-compose.yml.
# Copy to .env and override as needed: `cp .env.example .env`

# Admin password for the local TAP container. A baked-in default is used when
# this is unset, so local dev works without any setup; set it to avoid relying
# on the literal in docker-compose.yml.
TAP_ADMIN_PASSWORD=
