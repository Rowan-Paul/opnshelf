{
  "name": "opnshelf-mail-relay",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "SMTP→Cloudflare REST bridge so the Tranquil PDS can send email from Railway (where outbound SMTP is blocked).",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "mailparser": "^3.7.4",
    "smtp-server": "^3.14.0"
  }
}
